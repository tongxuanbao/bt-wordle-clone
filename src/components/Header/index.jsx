const Header = () => {
  return (
    <header className="Header">
      <button> rules</button>
      <h1>WORDLE CLONE</h1>
      <button>settings</button>
    </header>
  );
};

export default Header;
